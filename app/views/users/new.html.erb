<% content_for :title, "メンバーを追加" %>

<main class="users-new">
  <h1>メンバーを追加</h1>
  <%= render "layouts/settings_nav" %>

  <div class="card">
    <%= render "form", user: @user %>
  </div>

  <p><%= link_to "← メンバー一覧に戻る", users_path %></p>
</main>
