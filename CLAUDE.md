# 面接日程調整ツール

採用面接の日程調整を行う社内ツール。技術課題として実装中。

## 何を解決するツールか

採用担当者は、候補者からメールで届く希望日時に対して、社内メンバーのGoogleカレンダーを一人ずつ開いて空きを確認している。困りごとは「確認の工数」と「予定の重複」の2つだが、**原因は1つで、確認と予約が分断されていること**。確認した瞬間から登録するまでの間、誰も空き状況を見ていない。

**このツールの目的は、確認から予約までを一度の操作で完結させ、確認工数と重複を同時に消すこと。**

## 技術スタック

Ruby 3.2+ / Rails 8.1 / SQLite3 / ERB + Hotwire (Turbo) / Minitest

## 設計ドキュメント

| ファイル | 内容 |
|---|---|
| `docs/仕様書_MVP.md` | データモデル、判定ロジック、排他制御、画面仕様 |
| `docs/要件定義書_Phase別.md` | 機能一覧、Phase分け、業務ルール（BR-01〜BR-12） |
| `docs/別紙A_実装詳細.md` | 排他制御の実装、テスト方針、ルーティング |

**実装前に該当箇所を読むこと。** 特にデータモデルと空き判定は仕様書 §3・§4 に定義がある。

---

## 変えてはいけない設計判断

以下は理由があって決めたもの。**変更を提案する場合は、必ず理由とともに確認を取ること。**

### 1. 空き判定は `app/services/availability_checker.rb` に集約する

検索時と予約確定時の**両方から同じクラスを呼ぶ**。

判定をコントローラに書くと同じロジックが2箇所にでき、片方だけ修正されて食い違う。**このツールの重複防止は「確定直前に同じ判定をやり直す」仕組みで成立しているため、判定が2つあると防止そのものが壊れる。**

確定処理側で簡略化した判定（重なりだけ見る等）を書かないこと。

### 2. 予約確定は必ずトランザクション内で再判定する

```ruby
ActiveRecord::Base.transaction(isolation: :immediate) do
  # 1. 選択メンバー全員を AvailabilityChecker で再判定
  # 2. 1名でも不可なら ActiveRecord::Rollback（部分確定はしない）
  # 3. Interview / InterviewAttendee / CalendarEvent を作成
end
```

検索結果の表示から予約ボタンを押すまでに時間差があり、その間に他の予定が入りうる。**表示時のチェックだけでは防げない。** 詳細は仕様書 §5。

### 3. 予約すると `calendar_events` にも書き込む

`interviews` は業務の記録、`calendar_events` は時間の占有で**別の事実**。1件の面接から出席者の数だけ占有が生まれる。

`source: "app"` と `interview_id` を必ず設定する。**書かないと、次の検索で自ツールが入れた面接が空き判定から漏れ、自分の予定に自分でダブルブッキングする。**

### 4. 時刻は `Time.current` / `Time.zone.parse` のみ

`Time.now` と `Time.parse` は使わない。ActiveRecord は UTC で保存し JST で表示するため、混ぜると9時間ずれる。**エラーは出ず、判定結果だけが狂う。**

### 5. 区間の重なり判定に等号を含めない

```ruby
where("start_at < ? AND end_at > ?", 要求end, 要求start)
```

`<=` にしない。隣接する予定（10:00-11:00 の直後の 11:00-12:00）を空きと判定するため。**これは「面接前後のバッファなし」という仮定（A-8）と対応している。**

### 6. 空いていない人・枠も、理由付きで表示する

一覧から除外しない。相談者は現在、誰がなぜ空いていないかを目視で把握しており、**空いているものだけ表示するとその情報が失われる。**

例：「予定あり（部門定例）」「終日予定（有給）」「営業時間外」「予約不可時間（昼休み）」

### 7. N+1 を作らない

`AvailabilityChecker` はメンバーごとに判定するため、素朴に書くと N+1 になる。`includes(:calendar_events, :availability_rules)` で事前読み込みし、**Ruby側で絞り込む**。

週カレンダーでは週5日 × 営業8時間 × 5分刻み = 480区間を走査するため、**区間ごとにDBを叩くと致命的**になる。

---

## データモデル（5テーブル）

| テーブル | 役割 |
|---|---|
| `users` | メンバー。`operator`（ツールを操作する）と `interviewer`（面接に出る）の2フラグ。**兼任があるため役割で分けずフラグで持つ** |
| `calendar_events` | 時間の占有。`source` が `external`（ダミー予定）か `app`（本ツールが作成）か |
| `availability_rules` | 予約可能・不可の時間帯。`user_id` が NULL ならテナント全体、値があれば個人 |
| `interviews` | 面接の業務記録。候補者名・日時・場所・URL・`created_by_id` |
| `interview_attendees` | 面接と面接官の紐付け。`[interview_id, user_id]` に UNIQUE 制約 |

**候補者はテーブルにしない。** `interviews.candidate_name` の文字列。候補者マスタは選考プロセス管理の入口になり、日程調整ツールの範囲を超えるため。

**`availability_rules` に `scope` カラムは置かない。** `user_id` の NULL 判定で足りる。

---

## 業務ルール（抜粋）

| ID | ルール |
|---|---|
| BR-01 | 重なり判定は `既存開始 < 要求終了 かつ 既存終了 > 要求開始` |
| BR-02 | 隣接する予定は**空きとする**（等号を含めない） |
| BR-03 | 終日予定がある日は、その日全体を不可 |
| BR-04 | 要求区間は営業時間内に**完全に収まる**こと |
| BR-06 | 確定直前に再判定し、**1名でも不可なら予約全体を中止** |
| BR-07 | 面接官の承諾を待たず即時確定（仮押さえ状態を持たない） |
| BR-08 | 開始時刻は5分刻みで自由。所要時間は分単位で自由（プリセットは入力補助） |
| BR-11 | 予約タイトルは `面接：{候補者名}様` で**固定**。編集不可 |
| BR-12 | `allow` はテナントが上限、個人があれば積集合。`block` は和集合。**個人設定でテナントの制限は緩められない** |

全ルールは `docs/要件定義書_Phase別.md` §2-6。

---

## 現在の状況

**主動線を入れ替える改修中。**

```
旧：日時を入力 → 空きメンバー一覧 → 選んで予約
新：面接官を選ぶ → 週カレンダー表示 → 空き枠をクリック → 予約
```

追加ヒヤリング（H2-05）で「時間で絞るのではなく、参加メンバーのカレンダー一覧を見て空き時間を探せる方が便利」との要件が出たため。旧動線は**希望日時が全員埋まっていた場合に行き止まりになる**。

### 改修の影響範囲

| 層 | 影響 |
|---|---|
| `AvailabilityChecker` | **変更なし** |
| データモデル | **変更なし** |
| 排他制御 | **変更なし** |
| 画面①・ルーティング | 作り直し |

**判定は「このメンバーはこの区間に空いているか」を答えるだけで、呼び出し順序を知らない。** 旧動線は区間を固定してメンバーを回し、新動線はメンバーを固定して区間を回す。呼ぶ側のループが変わるだけ。

仕様書 §6.6 に新しい操作フローと空き枠の走査手順がある。

### 週カレンダーの表示方針

- 選択メンバーの予定を**1つのカレンダーに重ねる**（個人ごとに列を分けない）
- 探しているのは「全員が空いている枠」であり、1人でも埋まっていればその枠は使えないため
- 埋まっている区間には**誰の何の予定かをラベル表示**（「伊藤／部門定例」）

---

## 実装していないこと（意図的）

提案しないでよい。すべて Phase 2 以降。

- 候補者向けの予約ページ・仮押さえ・公開URL
- Googleカレンダーの実連携（OAuth / API）— ダミーデータで再現する合意がある
- 候補者へのメール送信
- 予約の変更・キャンセル
- 会議室の予約
- パスワード認証・権限制御（**簡易ログインはなりすまし可能。承知のうえ**）
- 面接官の属性フィルタ、承諾フロー、通知

## 未確定のまま仮定を置いている箇所

| # | 仮定 |
|---|---|
| A-1 | 「予定あり」は一律で不可。調整可能かは判定しない |
| A-2 | 1回の面接に面接官は1〜N名 |
| A-3 | 予約時に候補者へのメール送信はしない |
| A-5 | 成功の定義は「重複0件」と「空き確認時間の削減」 |
| A-6 | 管理者とCAの権限差は実装しない |
| A-7 | 終日予定はその日全体を不可 |
| A-8 | 面接前後のバッファなし |

---

## 作業の進め方

**この課題は「実装の完成度」を評価しない。** 評価されるのは、要件を整理できたか（30%）、設計の妥当性と説明可能性（25%）、**自分の成果物を説明できるか（25%）**、前提変更への対応（20%）。

そのため以下を守ること。

1. **大きな変更を一度に入れない。** 段階を区切り、都度何を変えたか説明する
2. **生成したコードは説明できる状態にする。** 最終レビューで任意の箇所を指されて質問される
3. **仕様書に書かれていない判断をした場合は、その旨を明示する。** 後で判断メモに記録する必要がある
4. **テストは境界条件を優先する。** 特に区間の重なり（仕様書 §4.4 の11ケース）

## 未検証の項目

- `transaction(isolation: :immediate)` が Rails 8.1 の SQLite3 アダプタで動作するか。非対応なら `connection.execute("BEGIN IMMEDIATE")` を直接発行する
- **過去日時での予約が可能になっていないか。** 空き判定は「予定と重なるか」しか見ておらず、過去日を指定すると全員空きと出る可能性がある
- `db/*.sqlite3` が `.gitignore` に入っているか
