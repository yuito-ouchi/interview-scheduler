# ミーティング調整ツール

社内の時間調整を行うツール。技術課題として実装中。**採用面接専用のツールではなく、汎用の日程調整ツール**（元は面接ユースケースを題材に作り始めたが、用語・ドメインを汎用の「ミーティング調整」に統一した）。

## 何を解決するツールか

主催者は、ゲストからメールで届く希望日時に対して、社内メンバーのGoogleカレンダーを一人ずつ開いて空きを確認している。困りごとは「確認の工数」と「予定の重複」の2つだが、**原因は1つで、確認と予約が分断されていること**。確認した瞬間から登録するまでの間、誰も空き状況を見ていない。

**このツールの目的は、確認から予約までを一度の操作で完結させ、確認工数と重複を同時に消すこと。**

## 技術スタック

Ruby 3.2+ / Rails 8.1 / SQLite3 / ERB + Hotwire (Turbo) / Minitest

## 設計ドキュメント

| ファイル | 内容 |
|---|---|
| `docs/仕様書_MVP.md` | データモデル、判定ロジック、排他制御、画面仕様 |
| `docs/要件定義書_Phase別.md` | 機能一覧、Phase分け、業務ルール（BR-01〜BR-12） |
| `docs/別紙A_実装詳細.md` | 排他制御の実装、テスト方針、ルーティング |
| `docs/判断メモ.md` | 仕様書・要件定義書に無い、または異なる実装判断（D-1〜D-12） |

**実装前に該当箇所を読むこと。** 特にデータモデルと空き判定は仕様書 §3・§4 に定義がある。

**ドキュメント内の「面接」「候補者」「面接官」「採用担当者」表記について**：設計ドキュメントは元々採用面接シナリオで書かれており、まだ完全にはミーティング用語へ統一できていない箇所が残っている（見つけ次第、参加者／ゲスト／主催者／ミーティングへ直す）。コード側（モデル・カラム・変数名）は統一済み。

---

## 変えてはいけない設計判断

以下は理由があって決めたもの。**変更を提案する場合は、必ず理由とともに確認を取ること。**

### 1. 空き判定は `app/services/availability_checker.rb` に集約する

検索時と予約確定時の**両方から同じクラスを呼ぶ**。

判定をコントローラに書くと同じロジックが2箇所にでき、片方だけ修正されて食い違う。**このツールの重複防止は「確定直前に同じ判定をやり直す」仕組みで成立しているため、判定が2つあると防止そのものが壊れる。**

確定処理側で簡略化した判定（重なりだけ見る等）を書かないこと。

### 2. 予約確定は必ずトランザクション内で再判定する

```ruby
ActiveRecord::Base.transaction do
  # 1. 選択メンバー全員を AvailabilityChecker で再判定
  # 2. 1名でも不可なら ActiveRecord::Rollback（部分確定はしない）
  # 3. Meeting / MeetingAttendee / CalendarEvent を作成
end
```

`isolation: :immediate` は使わない。Rails 8.1 の SQLite3 アダプタは `TransactionIsolationError` で拒否するが、このアダプタは全トランザクションを内部的に `BEGIN IMMEDIATE` で開始する仕様のため、素の `transaction` で当初の目的（開始時点での書き込みロック取得）を満たせる（検証済み。別紙A A-3）。

検索結果の表示から予約ボタンを押すまでに時間差があり、その間に他の予定が入りうる。**表示時のチェックだけでは防げない。** 詳細は仕様書 §5。

### 3. 予約すると `calendar_events` にも書き込む

`meetings` は業務の記録、`calendar_events` は時間の占有で**別の事実**。1件のミーティングから出席者の数だけ占有が生まれる。

`source: "app"` と `meeting_id` を必ず設定する。**書かないと、次の検索で自ツールが入れたミーティングが空き判定から漏れ、自分の予定に自分でダブルブッキングする。**

### 4. 時刻は `Time.current` / `Time.zone.parse` のみ

`Time.now` と `Time.parse` は使わない。ActiveRecord は UTC で保存し JST で表示するため、混ぜると9時間ずれる。**エラーは出ず、判定結果だけが狂う。**

### 5. 区間の重なり判定に等号を含めない

```ruby
where("start_at < ? AND end_at > ?", 要求end, 要求start)
```

`<=` にしない。隣接する予定（10:00-11:00 の直後の 11:00-12:00）を空きと判定するため。**これは「ミーティング前後のバッファなし」という仮定（A-8）と対応している。**

### 6. 空いていない人・枠も、理由付きで表示する

一覧から除外しない。相談者は現在、誰がなぜ空いていないかを目視で把握しており、**空いているものだけ表示するとその情報が失われる。**

例：「予定あり（部門定例）」「終日予定（有給）」「営業時間外」「予約不可時間（昼休み）」

### 7. N+1 を作らない

`AvailabilityChecker` はメンバーごとに判定するため、素朴に書くと N+1 になる。`includes(:calendar_events, :availability_rules)` で事前読み込みし、**Ruby側で絞り込む**。

週カレンダーでは週5日 × 営業8時間 × 5分刻み = 480区間を走査するため、**区間ごとにDBを叩くと致命的**になる。

---

## データモデル（5テーブル）

| テーブル | 役割 |
|---|---|
| `users` | メンバー。`operator`（ツールを操作する）と `participant`（ミーティングに出る）の2フラグ。**兼任があるため役割で分けずフラグで持つ** |
| `calendar_events` | 時間の占有。`source` が `external`（ダミー予定）か `app`（本ツールが作成）か |
| `availability_rules` | 予約可能・不可の時間帯。`user_id` が NULL ならテナント全体、値があれば個人 |
| `meetings` | ミーティングの業務記録。ゲスト名・日時・場所・URL・`created_by_id` |
| `meeting_attendees` | ミーティングと参加者の紐付け。`[meeting_id, user_id]` に UNIQUE 制約 |

**ゲストはテーブルにしない。** `meetings.guest_name` の文字列。ゲストマスタは選考・案件管理の入口になり、日程調整ツールの範囲を超えるため。

**`availability_rules` に `scope` カラムは置かない。** `user_id` の NULL 判定で足りる。

---

## 業務ルール（抜粋）

| ID | ルール |
|---|---|
| BR-01 | 重なり判定は `既存開始 < 要求終了 かつ 既存終了 > 要求開始` |
| BR-02 | 隣接する予定は**空きとする**（等号を含めない） |
| BR-03 | 終日予定がある日は、その日全体を不可 |
| BR-04 | 要求区間は営業時間内に**完全に収まる**こと |
| BR-06 | 確定直前に再判定し、**1名でも不可なら予約全体を中止** |
| BR-07 | 参加者の承諾を待たず即時確定（仮押さえ状態を持たない） |
| BR-08 | 開始時刻は5分刻みで自由。所要時間は分単位で自由（プリセットは入力補助） |
| BR-11 | 予約タイトルは `ミーティング：{ゲスト名}様` で**固定**。編集不可 |
| BR-12 | `allow` はテナントが上限、個人があれば積集合。`block` は和集合。**個人設定でテナントの制限は緩められない** |

全ルールは `docs/要件定義書_Phase別.md` §2-6。

---

## 現在の状況

**主動線の入れ替えは完了済み。** 画面①（ミーティングを組む）・①b（予約フォーム）が実装済みで、以下の流れで動く。

```
参加者を選ぶ → 週カレンダー表示 → 空き枠をクリック → 予約フォームで微調整 → 予約
```

追加ヒヤリング（H2-05）で「時間で絞るのではなく、参加メンバーのカレンダー一覧を見て空き時間を探せる方が便利」との要件が出たため（旧動線は希望日時が全員埋まっていた場合に行き止まりになっていた）。詳細は仕様書§1・§6.4・§6.6。

**この改修は `AvailabilityChecker`・データモデル・排他制御に影響していない。** 判定は「このメンバーはこの区間に空いているか」を答えるだけで呼び出し順序を知らないため、影響は画面①・ルーティングの作り直しに閉じた。

### 実装完了後にユーザー要望で追加したもの（仕様書に無かった判断。判断メモ参照）

- **UI刷新**：モダンなSaaS意匠（jicoo.com参照）にCSSを全面書き換え。デザイントークン化、トップバー、カード、サイドバー化。仕様書§6.8
- **「あと1人で空き」は追加後に撤回した**：全員空きの枠だけでなく1名だけ埋まっている枠もオレンジで表示する機能を仕様書§6.4・§6.6で追加したが、ニアミス枠が本人の「予定あり」ブロックと必ず同じ時間帯に重なり、人名が二重表示になるため削除した（`WeeklySlotFinder#near_windows` ／別紙A 第5版）
- **参加者の選び直しはサイドバーに一本化**：右サイドバーでチェック／所要時間を変更すると即座にカレンダーが再計算される（`onchange`でフォーム自動送信）。カレンダー本体の各予定ブロックからは操作できない（一度実装したが、状態がズレて混乱するため撤回した）
- **①bで開始時刻・所要時間を編集可能に**：BR-08（開始5分刻み・所要は分単位で自由）を満たすため、当初「①から引き継ぎ編集不可」だった開始時刻・所要時間を①bで自由入力にした
- **用語の全面統一**：もともと採用面接（interview / candidate / interviewer）を題材に実装していたが、「このツールは採用ツールではなく汎用の時間調整ツール」という指摘を受け、DB・モデル・コントローラー・ビュー・テストまで含めて「ミーティング（面接）／参加者（面接官）／ゲスト（候補者）／主催者（採用担当者）」に一括リネームした。テーブル名 `interviews`→`meetings`、`interview_attendees`→`meeting_attendees`、カラム `candidate_name`→`guest_name`、`users.interviewer`→`users.participant` を含む
- **認証・認可・予約管理の追加実装**（判断メモ D-1〜D-7）：簡易ログインをメール＋パスワード認証へ置き換え（当初 `has_secure_password`、後にD-12でDeviseへ移行）、`users.admin` フラグと `require_admin` で設定画面を保護。F-24 予約一覧・F-01/F-02 メンバー管理（画面③／admin限定）を実装。要件上 Phase 2 だった F-25 予約変更・F-26 キャンセルも前倒しで実装（参加者の変更は含めず、キャンセルして組み直す運用）。判定ロジック・排他制御・データモデルには波及していない
- **認証をDeviseへ移行し、主催者/参加者の自己登録を追加**（判断メモ D-12）：D-1で「Gemは不要」としていた判断をユーザー要望で見直した。`devise` ＋ `devise-i18n`（日本語化）。ルートは `/users`（既存の管理者用メンバー管理CRUD）との衝突を避け `/auth` 以下に分離。`admin` は自己登録では絶対に付与しない（`configure_permitted_parameters` の許可リストに含めていない）。自己アカウント削除はD-8と同じ理由で無効化し、管理者のメンバー管理に一本化した
- **削除時のデータ整合性ガード**（判断メモ D-8）：今後のミーティングに出席予定の参加者は `UsersController#destroy` で削除をブロックする（登録者は元々 `restrict_with_exception` で守られていたが、出席者側は未対応だった）
- **予約変更（F-25）に週カレンダーを追加**（判断メモ D-9）：手入力だけだった編集フォームに画面①と同じ週カレンダーを添え、空き帯クリックで日時を選び直せるようにした。カレンダー描画時はこのミーティング自身の占有を除外する（`confirm_update` の再判定と同じ理由）
- **予約一覧（F-24）に横断検索を追加**（判断メモ D-10）：`?q=` でゲスト名・参加者名・登録者名のいずれかに一致する予約へ絞り込める
- **設定の子ページとしてテナント固定ブロックの編集画面を追加**（判断メモ D-11）：F-11〜F-13のうち、テナント全体の `block` ルール（`user_id: nil`）だけをCRUDできる `TenantBlocksController` を実装。営業時間（allow）の編集・個人ルールの編集は対象外だった（→ D-13 で統合）
- **予約変更（F-25）で参加者を選び直せるように**（判断メモ D-14）：D-5 で「キャンセルして組み直す運用」とした判断をユーザー要望で撤回。変更画面に画面①と同じ参加者チェックのサイドバーを置き、`confirm_update` の判定対象を「既存出席者」から「送信された参加者」に変えた。**再判定は従来どおり全員まとめて1回**（BR-06）で、出席者と占有の作り直しは `sync_attendees_and_events!` に閉じている
- **テナント設定を1画面に集約**（判断メモ D-13）：D-11 で対象外にした営業時間（allow・F-11）の編集を追加し、`TenantBlocksController` を `TenantRulesController`（`/tenant_rules`）へ統合。営業時間と固定ブロックは BR-12 で合成されて初めて予約可能時間が決まるため、タブを分けず1画面に2セクションで並べた。`rule_type` はURLのクエリと既存レコードだけで決まり、フォーム送信値では決まらない（`params.expect` の許可リストに入れていない）

### 週カレンダーの表示方針

- 選択メンバーの予定を**1つのカレンダーに重ねる**（個人ごとに列を分けない）
- 探しているのは「全員が空いている枠」であり、1人でも埋まっていればその枠は使えないため
- 埋まっている区間には**誰の何の予定かをラベル表示**（「伊藤／部門定例」）。複数人の予定が同じ時間帯に重なる場合は列を分けて横に並べる（`MeetingsHelper#layout_busy`）

### 実装時間内で未着手のMust／Should

カレンダー連携（画面④／F-29）は未実装。判定ロジックと主動線（画面①・①b）を優先した結果で、詳細は仕様書§9.4・要件定義書「Phase 1・実装時間内で未着手のMust／Should」を参照。メンバー管理（画面③）・予約一覧（画面②）は当初未着手だったが実装済み（判断メモ D-3・D-4）。テナント全体の設定（F-11 営業時間／F-12・F-13 固定ブロック）も `/tenant_rules` の1画面として実装済み（判断メモ D-11・D-13）。`availability_rules` のうち**個人ルール（`user_id` あり）だけ**が引き続き `db/seeds.rb` 投入か `rails console` でしか変更できない。

---

## 実装していないこと（意図的）

提案しないでよい。すべて Phase 2 以降。

- ゲスト向けの予約ページ・仮押さえ・公開URL
- Googleカレンダーの実連携（OAuth / API）— ダミーデータで再現する合意がある
- ゲストへのメール送信・変更/キャンセル通知
- 会議室の予約
- 多要素認証・監査ログ・「管理者／CA」の権限差の細分化（認証と `admin` による設定画面保護は実装済み。F-52）
- 参加者の属性フィルタ、承諾フロー、通知

## 未確定のまま仮定を置いている箇所

| # | 仮定 |
|---|---|
| A-1 | 「予定あり」は一律で不可。調整可能かは判定しない |
| A-2 | 1回のミーティングに参加者は1〜N名 |
| A-3 | 予約時にゲストへのメール送信はしない |
| A-5 | 成功の定義は「重複0件」と「空き確認時間の削減」 |
| A-6 | 管理者とCAの権限差は「設定画面に入れるか否か」の1段階のみ（`admin` フラグ）。それ以上は未実装 |
| A-7 | 終日予定はその日全体を不可 |
| A-8 | ミーティング前後のバッファなし |

---

## 作業の進め方

**この課題は「実装の完成度」を評価しない。** 評価されるのは、要件を整理できたか（30%）、設計の妥当性と説明可能性（25%）、**自分の成果物を説明できるか（25%）**、前提変更への対応（20%）。

そのため以下を守ること。

1. **大きな変更を一度に入れない。** 段階を区切り、都度何を変えたか説明する
2. **生成したコードは説明できる状態にする。** 最終レビューで任意の箇所を指されて質問される
3. **仕様書に書かれていない判断をした場合は、その旨を明示する。** 後で判断メモに記録する必要がある
4. **テストは境界条件を優先する。** 特に区間の重なり（仕様書 §4.4 の11ケース）

## 検証済みの項目（旧「未検証の項目」）

- `transaction(isolation: :immediate)` は Rails 8.1 の SQLite3 アダプタでは**非対応**（`TransactionIsolationError`）。ただしこのアダプタは全トランザクションを内部的に `BEGIN IMMEDIATE` で開始するため、素の `transaction do...end` で当初の目的（開始時点での書き込みロック取得）は満たせる。別紙A A-3参照
- **過去日時での予約は `Meeting` のバリデーションで弾く実装済み**（`validates :start_at, comparison: { greater_than: -> { Time.current } }, on: :create`）。テスト「create：過去日時は弾く（§9.3-1）」で検証している
- `db/*.sqlite3` ではなく Rails 8 標準の `storage/*.sqlite3` に生成され、`.gitignore` の `/storage/*` で除外されることを確認済み
